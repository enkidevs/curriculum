name: Sum Target Value From a Given List

description: Find which two numbers of the given `numbers` list, when summed up, equal the `target` value. Return their indices.

aspects:
  - workout

insights:
  - sum-target-value-from-a-list